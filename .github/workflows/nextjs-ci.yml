name: nextjs-ci
on:
  push:
    paths:
      - "frontends/app-nextjs/**"
      - ".github/workflows/nextjs-ci.yml"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontends/app-nextjs
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install
      - run: bun run build
