version: "2"
sql:
  - engine: postgresql
    schema: migrations      # the single source of truth
    queries: queries
    gen:
      go:
        package: db
        out: ../backend/internal/db   # generated code lives in the backend
        sql_package: pgx/v5
        emit_json_tags: true
        emit_interface: true
